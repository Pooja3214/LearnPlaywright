var name="Pooja";
var firstName="Pooja";
var lastName="Sharma";  //Camel Case

var first_name="Pooja"; // Snake Case

//Naming convention cASES
//===========================================
//1. camelCase(Standard for JS variables and functions)
let userName="camelCase";
let totalPrice=99.99;
let isLoggedIn=true;

//2. PascelCase(Standard for JS classes and constructors)
let UserProfile="PascalCase";
let shoppingCart="class name style";

//3. snake_case(underscrore seperated)
let user_name="snake_case";
let total_price=49.99;
let is_logged_in=false;

//4. SCREAMING_SNAKE_CASE(uppercase with underscores, often used for constants)
const MAX_SIZE=100;
const API_KEY="abc123";
const DATA_BASE_URL="localhost";

//5. Hungarian Notation(prefixes to indicate type- older style or purpose, less common in modern JS)
let strName="string prefix";
let bIsActive=true; //boolean prefix
let nCount=10; // number prefix
let arrItems=[1,2,3];// array prefix
