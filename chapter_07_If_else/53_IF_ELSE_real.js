let userName="Pooja";
let password="secure123";
let isAccountLocked=true;

// logical operator + if -else
    if((userName==="Pooja" && password==="secure123") && isAccountLocked)
    {
console.log("Allowed to enter")
    }else{
        console.log("Not allowed to enter");
    }