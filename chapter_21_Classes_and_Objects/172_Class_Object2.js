class Person {
    constructor() {
        console.log("I will be created when Obj is created!")
    }
    // Attributes
    name;
    email;
    salary;
    address;

    // Behaviour
    sleep() { }
    eat() { }
    walk() { }


}

  let obj_ref=new Person();
//   obj_ref- It is the reference of the object address
//   new Person()- It is the object with new Keyword

// console.log(obj_ref);
